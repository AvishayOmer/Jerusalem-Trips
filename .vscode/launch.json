{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Open in Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}"
        }
    ]
}